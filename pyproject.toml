[project]
name = "collar-lz-harness-tools"
version = "0.1.0"
description = "Python tooling for LayerZero harness deployment and diagnostics"
requires-python = ">=3.10"
dependencies = [
  "typer>=0.12.5",
  "rich>=13.9.4",
  "python-dotenv>=1.0.1",
  "web3>=7.6.0",
  "eth-account>=0.13.5",
]

[tool.uv]
package = false
